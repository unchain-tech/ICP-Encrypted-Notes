type EncryptedNote = record {
  "id" : nat;
  "encrypted_text" : text;
}

service : {
  "getNotes" : () -> (vec EncryptedNote) query;
  "addNote" : (text) -> ();
  "deleteNote" : (nat) -> ();
  "updateNote" : (EncryptedNote) -> ();
};
