import { memo, FC } from 'react'
import Layout from '../components/layout/Layout'

const DeviceList: FC = memo(() => {
  return (
    <Layout>
      <h1>DeviceList Page</h1>
    </Layout>
  )
})

export default DeviceList