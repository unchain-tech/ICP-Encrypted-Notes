import { memo, FC } from 'react'

import Layout from '../components/layout/Layout'

const Login: FC = memo(() => {
  return (
    <Layout>
      <h1>Login Page</h1>
    </Layout>
  )
})

export default Login